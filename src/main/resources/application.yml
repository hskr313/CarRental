spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/CarRental
    username: postgres
    password: root
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  webflux:
    format:
      date: dd/MM/yyyy